<nav class="navbar navbar-expand-lg navbar-dark">
    <a class="navbar-brand" href="{{ path("sortie_list") }}">Sortir.com</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            {% if is_granted('IS_AUTHENTICATED_FULLY') %}
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle " style="cursor: pointer;" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Sorties
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="{{ path("sortie_create") }}">Créer une sortie</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="{{ path("messorties_list") }}">Gérer mes sorties</a>
                    </div>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("account_profile") }}">Mon profil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_logout") }}">Se déconnecter</a>
                </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("user_register") }}">Enregistrer un utilisateur</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("manage_site") }}">Gérer les sites</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("manage_city") }}">Gérer les villes</a>
                </li>
            {% endif %}
            {% else %}
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("app_login") }}">Se connecter</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path("sortie_list") }}">Sorties</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>