{% extends 'base.html.twig' %}

{% block title %}Création d'un utilisateur{% endblock %}

{% block main %}
    <h1>Création d'un utilisateur</h1>
    {{ form_start(registerForm) }}
    {{ form_widget(registerForm) }}
    <button type="submit" class="btn btn-outline-dark">{{ "Enregistrer" |trans }}</button>
    {{ form_start(registerForm) }}
    <button type=""  style="" class="btn btn-outline-dark" onclick="window.history.go(-1)" >Retour</button>
{% endblock %}


